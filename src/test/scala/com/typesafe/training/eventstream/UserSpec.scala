package com.typesafe.training.eventstream

import com.typesafe.training.hakkyhour.BaseSpec
import akka.testkit.{ EventFilter, TestActorRef }

class UserSpec extends BaseSpec("user") {
//
//  "User" should {
//    "store events in reverse chronological order" in {
//      val sessionId = 999L
//      val user = TestActorRef(new User(sessionId))
//      EventFilter.info(pattern = ".*[Ee]njoy.*", occurrences = 1) intercept {
//        user ! User.AddEventToHistory(request)
//      }
//    }
//  }
}
